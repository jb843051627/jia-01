<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fc.v2.mapper.auto.TOrderItemMapper">

    <resultMap type="com.fc.v2.model.auto.TOrderItem" id="TOrderItemResult">
        <result property="id"    column="id"    />
        <result property="orderId"    column="order_id"    />
        <result property="serviceTypeId"    column="service_type_id"    />
        <result property="serviceName"    column="service_name"    />
        <result property="price"    column="price"    />
        <result property="quantity"    column="quantity"    />
        <result property="subtotal"    column="subtotal"    />
    </resultMap>

    <sql id="selectTOrderItemVo">
        select id, order_id, service_type_id, service_name, price, quantity, subtotal
        from t_order_item
    </sql>

    <select id="selectOrderItemsByOrderId" resultMap="TOrderItemResult" parameterType="Long">
        <include refid="selectTOrderItemVo"/>
        <where>
            order_id = #{orderId}
        </where>
        order by id asc
    </select>

    <delete id="deleteOrderItemsByOrderId" parameterType="Long">
        delete from t_order_item where order_id = #{orderId}
    </delete>

</mapper>
